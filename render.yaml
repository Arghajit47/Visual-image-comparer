services:
  - type: web
    name: visual-image-comparer-api
    runtime: docker
    region: oregon
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: ALLOWED_ORIGIN
        sync: false
    healthCheckPath: /api/health
